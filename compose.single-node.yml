services:
  slimvector:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      Storage__Path: /data
      Storage__FlushToDisk: "true"
      Raft__Mode: SingleNode
    volumes:
      - slimvector-data:/data
volumes:
  slimvector-data:
