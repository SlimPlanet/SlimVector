apiVersion: v1
kind: ConfigMap
metadata:
  name: slimvector-settings
  namespace: slimvector-eu-central
data:
  ASPNETCORE_URLS: "http://+:8180"
  Storage__Path: "/data"
  Storage__FlushToDisk: "true"
  DiskAnn__Path: "/data/indexes/diskann"
  Backup__Path: "/data/backups"
  Raft__Mode: "Cluster"
  Raft__PublicApiEndpoint: "http://127.0.0.1:8091"
  Raft__DataGroupCount: "2"
  Raft__DataPortRangeStart: "3363"
  Raft__DataPortRangeCount: "16"
  Raft__CapacityBytes: "5368709120"
  Raft__ElectionTimeout: "00:00:03"
  Raft__HeartbeatInterval: "00:00:00.500"
  ClusterMembership__Enabled: "true"
  DataPlacement__ReplicationFactor: "3"
  DataPlacement__MinimumGroupReplicasPerNode: "1"
  Rebalancing__Enabled: "true"
  Rebalancing__ManualApproval: "true"
  Api__AdminEndpointsEnabled: "true"
  GeoReplication__Enabled: "false"
  GeoReplication__AcceptIncoming: "true"
  GeoReplication__ReadOnlySecondary: "true"
  GeoReplication__StatePath: "/data/geo-replication"
  Observability__ServiceName: "SlimVector-eu-central"
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: slimvector
  namespace: slimvector-eu-central
  labels:
    app.kubernetes.io/name: slimvector
    app.kubernetes.io/part-of: slimvector
    slimvector.dev/region: eu-central
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: slimvector
      slimvector.dev/region: eu-central
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: slimvector
        app.kubernetes.io/part-of: slimvector
        slimvector.dev/region: eu-central
    spec:
      nodeSelector:
        slimvector.dev/worker: "true"
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 1654
        runAsGroup: 1654
        fsGroup: 1654
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: slimvector
          image: localhost/slimvector:kube-local
          imagePullPolicy: Never
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          envFrom:
            - configMapRef:
                name: slimvector-settings
            - configMapRef:
                name: slimvector-members
          env:
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: Raft__NodeId
              value: "$(HOST_IP)"
            - name: Raft__PublicEndpoint
              value: "http://$(HOST_IP):3362"
            - name: Raft__InternalEndpoint
              value: "http://$(HOST_IP):8180"
            - name: Raft__Zone
              value: "$(HOST_IP)"
            - name: GeoReplication__OriginId
              value: "eu-central-$(HOST_IP)"
            - name: Api__AdminApiKey
              valueFrom:
                secretKeyRef:
                  name: slimvector-secrets
                  key: admin-api-key
            - name: GeoReplication__SharedSecret
              valueFrom:
                secretKeyRef:
                  name: slimvector-secrets
                  key: geo-shared-secret
            - name: TMPDIR
              value: "/tmp"
          ports:
            - name: http
              containerPort: 8180
              protocol: TCP
            - name: raft-catalog
              containerPort: 3362
              protocol: TCP
            - name: raft-data-0
              containerPort: 3363
              protocol: TCP
            - name: raft-data-1
              containerPort: 3364
              protocol: TCP
          startupProbe:
            httpGet:
              path: /health/live
              port: http
            failureThreshold: 60
            periodSeconds: 2
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 2
            periodSeconds: 3
            timeoutSeconds: 2
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 768Mi
          volumeMounts:
            - name: data
              mountPath: /data
            - name: temporary
              mountPath: /tmp
      volumes:
        - name: data
          hostPath:
            path: /var/lib/slimvector/eu-central
            type: Directory
        - name: temporary
          emptyDir:
            sizeLimit: 128Mi
